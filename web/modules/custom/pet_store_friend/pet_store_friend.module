<?php
  /**
  * Implements hook_theme().
  */
  function pet_store_friend_theme($existing, $type, $theme, $path) { 
  
    return array(
      'article_list' => array('variables' => array('items' => array(), 'title' => '')),
    );
    
  }